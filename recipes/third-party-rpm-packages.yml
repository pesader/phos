# Third-party RPM packages ---------------------------------------------------

modules:
  # hblock
  - type: dnf
    repos:
      copr:
        - pesader/hblock
    install:
      - hblock

  # showmethekey
  - type: dnf
    repos:
      copr:
        - pesader/showmethekey
    install:
      - showmethekey
