# Bundles --------------------------------------------------------------------

modules:
  # Vaults -------------------------------------------------------------------

  # gocrypts RPM package
  - type: dnf
    repos:
      copr:
        - nucleo/gocryptfs
    install:
      packages:
        - gocryptfs

  # Vaults flatpak app
  - type: default-flatpaks
    configurations:
      - notify: false
        install:
          - io.github.mpobaschnig.Vaults
