# Bundles --------------------------------------------------------------------

modules:
  # Vaults (depends on gocryptfs)
  - type: dnf
    repos:
      copr:
        - pesader/gocryptfs
    install:
      packages:
        - gocryptfs
  - type: default-flatpaks
    configurations:
      - notify: false
        install:
          - io.github.mpobaschnig.Vaults
